import { SecondsflagPipe } from './secondsflag.pipe';

describe('SecondsflagPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsflagPipe();
    expect(pipe).toBeTruthy();
  });
});
